# POSIX target

Runs the exact sample FreeRTOS application against the deterministic POSIX driver
backend. It validates composition and concurrency behavior; it is not evidence of
embedded interrupt latency or real-time performance.
